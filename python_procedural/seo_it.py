



# Get the n top results from a google request. Returns a list of url

# PAGE SCALE FUNCTIONS 

## parse a web page. Returns it as an html object or error (identify language)

## extract entity types: returns it as a Json

## get al links of the page: returns it as a (list?)

## get top 20 bigrams: returns it as a list (tweak nltk to use the relavant langage dependencies)

# DOMAIN SCALE FUNCTIONS

# look for robot.txt, follow internal links (by adding them on a stack) , parse all this mess
# stop when there is no more internal urls and return the results as a json


# apply this to all n top resuslts
def main(request): 
	pass


if __name__ == '__main__':
	main()